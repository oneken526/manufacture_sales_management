# TASK-0004: 共通 Blade レイアウト・ロール別ナビゲーション

**タスクID**: TASK-0004
**タスクタイプ**: TDD
**推定工数**: 8時間
**フェーズ**: Phase 1 - 認証・基盤・マスタ管理
**信頼性レベル**: 🔵 *要件定義書・設計文書より*

## 関連文書
- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/manufacturing-sales-mgmt/requirements.md)
- **設計文書**: [📐 architecture.md](../../design/manufacturing-sales-mgmt/architecture.md)
- **DBスキーマ**: [🗄️ database-schema.sql](../../design/manufacturing-sales-mgmt/database-schema.sql)
- **Webルート仕様**: [🔌 api-endpoints.md](../../design/manufacturing-sales-mgmt/api-endpoints.md)

## タスク概要
全画面で使用する共通 Blade レイアウト（`layouts/app.blade.php`）とロール別サイドバーナビゲーションを実装する。ロールに応じて表示するメニューを制御し、ゲスト用レイアウト（`layouts/guest.blade.php`）も整備する。`DashboardController` を作成してロール別にビューを切り替える。

## 依存タスク
- **前提タスク**: TASK-0003
- **後続タスク**: TASK-0005, TASK-0006, TASK-0007, TASK-0008, TASK-0009

## 完了条件
- [ ] `layouts/app.blade.php` が存在し、ヘッダー・サイドバー・コンテンツエリアを持つ
- [ ] ロール別サイドバーが正しいメニューのみ表示する（REQ-011）
- [ ] `layouts/guest.blade.php` が存在する
- [ ] `DashboardController@index` がロールに応じたビューを返す
- [ ] 未認証アクセスが `/login` にリダイレクトされる（REQ-012）
- [ ] すべての Feature テストがパスする

---

## 実装詳細

### 1. resources/views/layouts/app.blade.php 作成 🔵
**信頼性**: 🔵 *REQ-011（ロール別ナビゲーション）より*

全認証済みページの共通レイアウト。Tailwind CSS 4.0 と jQuery を読み込む。

```blade
{{-- resources/views/layouts/app.blade.php --}}
<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="csrf-token" content="{{ csrf_token() }}">
    <title>@yield('title', '製造業販売管理システム')</title>
    @vite(['resources/css/app.css', 'resources/js/app.js'])
</head>
<body class="bg-gray-100 font-sans">
    {{-- ヘッダー --}}
    <header class="bg-white shadow-sm h-16 flex items-center px-6 justify-between">
        <div class="text-lg font-bold text-gray-800">製造業販売管理システム</div>
        <div class="flex items-center gap-4">
            <span class="text-sm text-gray-600">{{ auth()->user()->name }}</span>
            <form method="POST" action="{{ route('logout') }}">
                @csrf
                <button type="submit" class="text-sm text-red-500 hover:underline">
                    ログアウト
                </button>
            </form>
        </div>
    </header>

    <div class="flex">
        {{-- サイドバー（ロール別） --}}
        <nav class="w-64 bg-white shadow-md min-h-screen pt-4">
            @include('layouts.partials.sidebar')
        </nav>

        {{-- メインコンテンツ --}}
        <main class="flex-1 p-6">
            @if(session('success'))
                <div class="mb-4 p-4 bg-green-100 text-green-800 rounded">
                    {{ session('success') }}
                </div>
            @endif
            @if(session('error'))
                <div class="mb-4 p-4 bg-red-100 text-red-800 rounded">
                    {{ session('error') }}
                </div>
            @endif
            @yield('content')
        </main>
    </div>
</body>
</html>
```

**実装ファイル**: `resources/views/layouts/app.blade.php`

---

### 2. resources/views/layouts/partials/sidebar.blade.php 作成 🔵
**信頼性**: 🔵 *REQ-011（ロール別メニュー表示制御）より*

ロール別にサイドバーのメニューを制御する。Spatie Permission の `@role` / `@hasanyrole` ディレクティブを使用する。

```blade
{{-- resources/views/layouts/partials/sidebar.blade.php --}}
<ul class="space-y-1 px-2">

    {{-- admin: 全メニュー --}}
    @role('admin')
        <li><a href="{{ route('dashboard') }}" class="block px-4 py-2 rounded hover:bg-gray-100">ダッシュボード</a></li>
        <li class="mt-2 text-xs text-gray-400 px-4 uppercase">マスタ管理</li>
        <li><a href="{{ route('customers.index') }}" class="block px-4 py-2 rounded hover:bg-gray-100">得意先管理</a></li>
        <li><a href="{{ route('products.index') }}" class="block px-4 py-2 rounded hover:bg-gray-100">商品管理</a></li>
        <li><a href="{{ route('warehouses.index') }}" class="block px-4 py-2 rounded hover:bg-gray-100">倉庫管理</a></li>
        <li><a href="{{ route('users.index') }}" class="block px-4 py-2 rounded hover:bg-gray-100">ユーザー管理</a></li>
        <li class="mt-2 text-xs text-gray-400 px-4 uppercase">販売管理</li>
        <li><a href="{{ route('quotations.index') }}" class="block px-4 py-2 rounded hover:bg-gray-100">見積管理</a></li>
        <li><a href="{{ route('orders.index') }}" class="block px-4 py-2 rounded hover:bg-gray-100">受注管理</a></li>
        <li class="mt-2 text-xs text-gray-400 px-4 uppercase">製造</li>
        <li><a href="{{ route('manufacture-orders.index') }}" class="block px-4 py-2 rounded hover:bg-gray-100">製造指示</a></li>
        <li class="mt-2 text-xs text-gray-400 px-4 uppercase">倉庫・出荷</li>
        <li><a href="{{ route('shipments.index') }}" class="block px-4 py-2 rounded hover:bg-gray-100">出荷管理</a></li>
        <li><a href="{{ route('stocks.index') }}" class="block px-4 py-2 rounded hover:bg-gray-100">在庫管理</a></li>
        <li class="mt-2 text-xs text-gray-400 px-4 uppercase">請求・入金</li>
        <li><a href="{{ route('invoices.index') }}" class="block px-4 py-2 rounded hover:bg-gray-100">請求管理</a></li>
        <li><a href="{{ route('payments.index') }}" class="block px-4 py-2 rounded hover:bg-gray-100">入金管理</a></li>
    @endrole

    {{-- sales: 見積・受注・得意先・レポート --}}
    @role('sales')
        <li><a href="{{ route('dashboard') }}" class="block px-4 py-2 rounded hover:bg-gray-100">ダッシュボード</a></li>
        <li class="mt-2 text-xs text-gray-400 px-4 uppercase">販売管理</li>
        <li><a href="{{ route('customers.index') }}" class="block px-4 py-2 rounded hover:bg-gray-100">得意先管理</a></li>
        <li><a href="{{ route('quotations.index') }}" class="block px-4 py-2 rounded hover:bg-gray-100">見積管理</a></li>
        <li><a href="{{ route('orders.index') }}" class="block px-4 py-2 rounded hover:bg-gray-100">受注管理</a></li>
    @endrole

    {{-- manufacture: 製造指示のみ --}}
    @role('manufacture')
        <li><a href="{{ route('dashboard') }}" class="block px-4 py-2 rounded hover:bg-gray-100">ダッシュボード</a></li>
        <li class="mt-2 text-xs text-gray-400 px-4 uppercase">製造</li>
        <li><a href="{{ route('manufacture-orders.index') }}" class="block px-4 py-2 rounded hover:bg-gray-100">製造指示</a></li>
    @endrole

    {{-- warehouse: 出荷・在庫 --}}
    @role('warehouse')
        <li><a href="{{ route('dashboard') }}" class="block px-4 py-2 rounded hover:bg-gray-100">ダッシュボード</a></li>
        <li class="mt-2 text-xs text-gray-400 px-4 uppercase">倉庫・出荷</li>
        <li><a href="{{ route('shipments.index') }}" class="block px-4 py-2 rounded hover:bg-gray-100">出荷管理</a></li>
        <li><a href="{{ route('stocks.index') }}" class="block px-4 py-2 rounded hover:bg-gray-100">在庫管理</a></li>
    @endrole

</ul>
```

**実装ファイル**: `resources/views/layouts/partials/sidebar.blade.php`

---

### 3. resources/views/layouts/guest.blade.php 作成 🔵
**信頼性**: 🔵 *Laravel Breeze 標準構成より*

ログイン・パスワードリセット用のシンプルなレイアウト。

```blade
{{-- resources/views/layouts/guest.blade.php --}}
<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@yield('title', '製造業販売管理システム')</title>
    @vite(['resources/css/app.css', 'resources/js/app.js'])
</head>
<body class="bg-gray-50 min-h-screen flex items-center justify-center">
    <div class="w-full max-w-md">
        <h1 class="text-center text-2xl font-bold text-gray-800 mb-8">
            製造業販売管理システム
        </h1>
        {{ $slot }}
    </div>
</body>
</html>
```

**実装ファイル**: `resources/views/layouts/guest.blade.php`

---

### 4. DashboardController 作成 🔵
**信頼性**: 🔵 *REQ-004（ロール別ダッシュボード）より*

ロールに応じてビューを切り替える。詳細なダッシュボードコンテンツは TASK-0006 で実装する。

```php
// app/Http/Controllers/DashboardController.php
<?php

declare(strict_types=1);

namespace App\Http\Controllers;

use Illuminate\Http\Request;
use Illuminate\View\View;

class DashboardController extends Controller
{
    /**
     * ダッシュボードを表示する
     * ログインユーザーのロールに応じてビューを切り替える
     */
    public function index(Request $request): View
    {
        $user = $request->user();

        return match (true) {
            $user->hasRole('admin')       => view('dashboard.admin'),
            $user->hasRole('sales')       => view('dashboard.sales'),
            $user->hasRole('manufacture') => view('dashboard.manufacture'),
            $user->hasRole('warehouse')   => view('dashboard.warehouse'),
            default                       => view('dashboard.admin'),
        };
    }
}
```

**実装ファイル**: `app/Http/Controllers/DashboardController.php`

---

### 5. routes/web.php へのダッシュボードルート追加 🔵
**信頼性**: 🔵 *REQ-012（認証必須）より*

```php
// routes/web.php
use App\Http\Controllers\DashboardController;

Route::middleware(['auth'])->group(function () {
    Route::get('/dashboard', [DashboardController::class, 'index'])->name('dashboard');
});
```

**実装ファイル**: `routes/web.php`

---

## テスト要件（Feature テスト）

### テストケース1: admin ログイン後にダッシュボードへリダイレクト 🔵
**信頼性**: 🔵 *REQ-011・REQ-012 より*
**Given**: admin ロールを持つユーザーが存在する
**When**: `POST /login` で正しい認証情報を送信する
**Then**: `/dashboard` へリダイレクトされ、全メニューが表示される
**テストファイル**: `tests/Feature/DashboardTest.php`

### テストケース2: sales ロールは製造指示メニューが表示されない 🔵
**信頼性**: 🔵 *REQ-011（ロール別メニュー制御）より*
**Given**: sales ロールを持つユーザーがログイン済み
**When**: `GET /dashboard` にアクセスする
**Then**: 製造指示メニューが HTML に含まれない
**テストファイル**: `tests/Feature/DashboardTest.php`

### テストケース3: 未認証アクセスは /login へリダイレクト 🔵
**信頼性**: 🔵 *REQ-012（認証必須）より*
**Given**: ログインしていない状態
**When**: `GET /dashboard` にアクセスする
**Then**: `/login` へ HTTP 302 リダイレクトされる
**テストファイル**: `tests/Feature/DashboardTest.php`

---

## 実装手順

### TDD タスクの場合
1. `/tsumiki:tdd-requirements TASK-0004` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 注意事項
- Spatie Permission の `@role` ディレクティブを使用するため、`config/app.php` に ServiceProvider が登録されていることを確認すること
- Tailwind CSS のサイドバーリンクはアクティブ状態（現在ページ）のスタイルを `request()->routeIs()` で制御すること
- `layouts/guest.blade.php` は Breeze が生成したものをカスタマイズするか、上書きすること
- コンポーネントスロット（`$slot`）を使用する場合は Blade コンポーネントとして定義すること
- テストでは `actingAs($user)` を使用してロール別のアクセス制御を確認すること

---

## 信頼性レベルサマリー
- 🔵 青信号: 8件 (100%)
- 🟡 黄信号: 0件 (0%)
- 🔴 赤信号: 0件 (0%)
**品質評価**: ✅ 高品質
